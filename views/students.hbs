<div class="row">
    <div class="col-md-12">
        <br>
        <h2>Students</h2>
        <hr />

        <p>TODO: render a list of all student first and last names here
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>Student Number</th>
                            <th>Full name</th>
                            <th>email</th>
                            <th>Address</th>
                            <th>Status</th>
                            <th>Course ID</th>
                        </tr>
                    </thead>
                    <tbody>
                    {{#each data}}
                        <tr>
                            <td>{{studentNum}}</td>
                            <td><a href="/student/:{{studentNum}}"> {{firstName}} {{lastName}} </a></td>
                            <td>{{email}}</td>
                            <td>{{addressStreet}} {{addressCity}} {{addressProvince}} </td>
                            <td>{{status}} </td>
                            <td><a href="/students?course={{course}}"> {{course}} </a></td>
                        </tr>
                    {{else}}
                        <tr>
                            <td colspan="4">No DATA AVAILABLE</td>
                            {{!-- <div class="col-md-12 text-center">
                                <strong>{{message}}</strong>
                            </div> --}}

                        </tr>
                    {{/each}}
                    </tbody>
                </table>
            </div>

            
        </p>

    </div>
</div>
